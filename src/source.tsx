import React from "react"

const render = (fc: React.JSX.Element) => { }

// Do not remove the below comment
// <- Start Here ->

function App() {
    return <div>
        <p> Hello World </p>
    </div>
}
render(<App />)